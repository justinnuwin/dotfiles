# ansible/ansible-core/docker need no markers -- pip's Requires-Python picks a
# build for the active interpreter (ansible 6/core 2.13 on py3.8, 14/2.21 on py3.14).
ansible>=6.0.0
ansible-core>=2.12.0
docker>=7.0.0

# molecule/ansible-lint dropped py3.8 and need core >=2.20 (py3.12+), so gate the
# modern floors to 3.12+ and let pip pick older compatible releases below that.
molecule>=6.0.0 ; python_version >= "3.12"
molecule-plugins[docker]>=22.0.0 ; python_version >= "3.12"
ansible-lint>=26.4.0 ; python_version >= "3.12"

molecule ; python_version < "3.12"
molecule-plugins[docker] ; python_version < "3.12"
ansible-lint ; python_version < "3.12"
